# Vendure Shop API URL (required)
# Server-side only (recommended for security)
VENDURE_SHOP_API_URL=http://localhost:3000/shop-api
# Or use public variant if needed client-side
# NEXT_PUBLIC_VENDURE_SHOP_API_URL=http://localhost:3000/shop-api

# Vendure Channel Token (optional, defaults to __default_channel__)
VENDURE_CHANNEL_TOKEN=__default_channel__
# Or use public variant if needed client-side
# NEXT_PUBLIC_VENDURE_CHANNEL_TOKEN=__default_channel__

# Site URL for metadata/SEO (optional, defaults to https://example.com)
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Stripe.js publishable key (safe to expose to the browser)
# Use a pk_test_... key during development.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me

# Address map providers (optional OpenStreetMap/Nominatim defaults shown)
# For production traffic, use a hosted provider or your own service and identify your app.
NEXT_PUBLIC_MAP_TILE_URL=https://tile.openstreetmap.org/{z}/{x}/{y}.png
NOMINATIM_REVERSE_URL=https://nominatim.openstreetmap.org/reverse
GEOCODING_USER_AGENT=Lumé/1.0 (+https://your-store.example; contact: maps@your-store.example)

# Cache Revalidation Secret (required for /api/revalidate endpoint)
# Generate a secure random string, e.g.: openssl rand -base64 32
REVALIDATION_SECRET=your-secure-random-string-here

# Site Name for metadata/SEO (optional, defaults to Lumé)
# NEXT_PUBLIC_SITE_NAME=Lumé

# Optional homepage Sale rail. Publish a Vendure collection with this slug.
# The section stays hidden when the collection does not exist or has no products.
NEXT_PUBLIC_SALE_COLLECTION_SLUG=sale

# Vendure Auth Token Cookie Name (optional, defaults to vendure-auth-token)
# VENDURE_AUTH_TOKEN_COOKIE=vendure-auth-token

# Vendure Auth Token Header Name (optional, defaults to vendure-auth-token)
# VENDURE_AUTH_TOKEN_HEADER=vendure-auth-token

# Vendure Channel Token Header Name (optional, defaults to vendure-token)
# VENDURE_CHANNEL_TOKEN_HEADER=vendure-token
